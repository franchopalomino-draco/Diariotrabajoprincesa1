<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>📊 Interpretación Escala CL-SPC</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Montserrat', sans-serif;
      background: linear-gradient(135deg, #f0f9ff 0%, #f8f4ff 100%);
    }
    .interpretation-card {
      transition: all 0.3s ease;
    }
    .interpretation-card:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 25px -5px rgba(0,0,0,0.1);
    }
  </style>
</head>
<body class="min-h-screen py-6 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="text-center mb-8">
      <h1 class="text-3xl font-bold text-purple-700">Escala de Clima Laboral CL-SPC</h1>
      <p class="text-gray-600 mt-2">Ingresa tu puntaje total (50–250) para obtener una interpretación completa y enviarla por WhatsApp</p>
    </div>

    <div class="bg-white rounded-xl shadow-lg p-6 mb-8">
      <div class="mb-5">
        <label for="scoreInput" class="block text-sm font-medium text-gray-700 mb-2">Puntaje total:</label>
        <input
          type="number"
          id="scoreInput"
          min="50"
          max="250"
          placeholder="Ej. 166"
          class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:outline-none text-center text-lg"
        />
      </div>
      <button
        id="checkBtn"
        class="w-full bg-purple-600 hover:bg-purple-700 text-white font-bold py-3 px-4 rounded-lg transition shadow-md"
      >
        Interpretar y preparar para WhatsApp
      </button>
    </div>

    <div id="resultSection" class="hidden space-y-6">
      <!-- Categoría -->
      <div class="interpretation-card bg-white rounded-xl shadow p-5 border-l-4" id="categoryCard"></div>

      <!-- Significado -->
      <div class="interpretation-card bg-white rounded-xl shadow p-5">
        <h3 class="font-bold text-lg text-gray-800 mb-2">🔍 ¿Qué significa este resultado?</h3>
        <p id="meaningText" class="text-gray-700"></p>
      </div>

      <!-- Factores evaluados -->
      <div class="interpretation-card bg-white rounded-xl shadow p-5">
        <h3 class="font-bold text-lg text-gray-800 mb-2">📊 Variables (Factores) que evalúa la escala</h3>
        <ul class="list-disc pl-5 space-y-1 text-gray-700">
          <li><strong>Autorrealización:</strong> Oportunidades de crecimiento, desarrollo personal y profesional.</li>
          <li><strong>Involucramiento Laboral:</strong> Compromiso con la organización y sus objetivos.</li>
          <li><strong>Supervisión:</strong> Apoyo, orientación y retroalimentación de los jefes.</li>
          <li><strong>Comunicación:</strong> Fluidez, claridad y acceso a la información relevante.</li>
          <li><strong>Condiciones Laborales:</strong> Remuneración, tecnología, recursos y ambiente físico.</li>
        </ul>
      </div>

      <!-- Recomendaciones -->
      <div class="interpretation-card bg-white rounded-xl shadow p-5">
        <h3 class="font-bold text-lg text-gray-800 mb-2">💡 Recomendaciones de mejora</h3>
        <p id="recommendationsText" class="text-gray-700"></p>
      </div>

      <!-- Botón de WhatsApp -->
      <div class="interpretation-card bg-white rounded-xl shadow p-5 text-center">
        <h3 class="font-bold text-lg text-gray-800 mb-3">📲 ¿Quieres enviar esta interpretación?</h3>
        <p class="text-gray-600 mb-4">Haz clic abajo para abrir WhatsApp con un mensaje listo para enviar al número <strong>+51984006844</strong>.</p>
        <div id="whatsappBtnContainer"></div>
      </div>

      <div class="text-xs text-gray-500 mt-6 text-center">
        Basado en el <em>Manual CL-SPC (Palma Carrillo, 2004)</em> – Muestra: 1,323 trabajadores de Lima Metropolitana.
      </div>
    </div>
  </div>

  <script>
    const interpretations = {
      "Muy desfavorable": {
        range: "50–89",
        color: "border-red-500",
        meaning: "El ambiente laboral es percibido de manera muy negativa. Existe una baja satisfacción con las condiciones de trabajo, supervisión, comunicación y oportunidades de desarrollo. Este nivel sugiere riesgos psicosociales significativos y posible desmotivación colectiva.",
        recommendations: "Se recomienda una intervención urgente: diagnóstico por factores, reuniones de retroalimentación segura, revisión de políticas de remuneración y supervisión, y programas de bienestar laboral. Priorizar la escucha activa y la participación de los trabajadores en la solución de problemas."
      },
      "Desfavorable": {
        range: "90–129",
        color: "border-orange-500",
        meaning: "La percepción del clima laboral es predominantemente negativa. Aunque hay algunos aspectos funcionales, predominan las insatisfacciones en áreas clave como comunicación, reconocimiento o condiciones laborales.",
        recommendations: "Implementar acciones correctivas: fortalecer canales de comunicación, capacitar a supervisores en liderazgo participativo, revisar equidad en remuneraciones y reconocer logros. Promover espacios de diálogo y participación en la toma de decisiones."
      },
      "Media": {
        range: "130–169",
        color: "border-yellow-500",
        meaning: "El clima laboral es percibido de forma ambivalente. Hay aspectos positivos, pero también áreas de mejora. No hay una clara tendencia hacia la satisfacción o insatisfacción, lo que puede indicar estancamiento o falta de impulso organizacional.",
        recommendations: "Fortalecer lo positivo y atender lo deficitario: identificar fortalezas mediante encuestas cualitativas, establecer metas claras de mejora, fomentar el desarrollo profesional y mejorar la retroalimentación. Incentivar la innovación y el trabajo en equipo."
      },
      "Favorable": {
        range: "170–209",
        color: "border-teal-500",
        meaning: "El ambiente laboral es percibido positivamente. Los trabajadores se sienten valorados, tienen buenas condiciones y una supervisión efectiva. Hay compromiso y fluidez en la comunicación, aunque aún hay margen de mejora.",
        recommendations: "Consolidar las buenas prácticas: reconocer públicamente los logros, invertir en desarrollo continuo, fomentar la autonomía y la creatividad. Asegurar que las buenas condiciones se mantengan incluso en periodos de cambio o presión."
      },
      "Muy favorable": {
        range: "210–250",
        color: "border-green-500",
        meaning: "El clima laboral es percibido de manera excelente. Existe un alto nivel de satisfacción, compromiso, apoyo mutuo y condiciones óptimas. La organización es vista como un entorno saludable, motivador y propicio para el desarrollo personal y colectivo.",
        recommendations: "Mantener y proteger este clima: documentar las buenas prácticas, formar líderes internos, compartir el modelo con otras áreas. Evitar la complacencia y seguir innovando en bienestar, inclusión y desarrollo profesional."
      }
    };

    function getCategory(score) {
      if (score >= 210) return "Muy favorable";
      if (score >= 170) return "Favorable";
      if (score >= 130) return "Media";
      if (score >= 90) return "Desfavorable";
      return "Muy desfavorable";
    }

    document.getElementById('checkBtn').addEventListener('click', () => {
      const input = document.getElementById('scoreInput');
      const score = parseInt(input.value);
      const resultSection = document.getElementById('resultSection');
      const categoryCard = document.getElementById('categoryCard');
      const meaningText = document.getElementById('meaningText');
      const recommendationsText = document.getElementById('recommendationsText');
      const whatsappBtnContainer = document.getElementById('whatsappBtnContainer');

      if (isNaN(score) || score < 50 || score > 250) {
        alert("Por favor ingresa un puntaje válido entre 50 y 250.");
        return;
      }

      const category = getCategory(score);
      const data = interpretations[category];

      categoryCard.className = `interpretation-card bg-white rounded-xl shadow p-5 border-l-4 ${data.color}`;
      categoryCard.innerHTML = `
        <h2 class="text-xl font-bold text-gray-800">Tu categoría: <span class="text-purple-600">${category}</span></h2>
        <p class="text-gray-600 mt-1">Rango: ${data.range} • Puntaje: ${score}/250</p>
      `;

      meaningText.textContent = data.meaning;
      recommendationsText.textContent = data.recommendations;

      // Mensaje para WhatsApp
      const fullMessage = `📊 *INTERPRETACIÓN ESCALA CLIMA LABORAL CL-SPC*\n\n` +
        `Puntaje total: ${score}/250\n` +
        `Categoría: ${category}\n\n` +
        `🔍 *Significado:*\n${data.meaning}\n\n` +
        `💡 *Recomendaciones:*\n${data.recommendations}\n\n` +
        `📌 Basado en el Manual CL-SPC (Palma Carrillo, 2004)`;

      const encodedMessage = encodeURIComponent(fullMessage);
      const whatsappUrl = `https://wa.me/51984006844?text=${encodedMessage}`;

      whatsappBtnContainer.innerHTML = `
        <a href="${whatsappUrl}" target="_blank" class="inline-block bg-green-500 hover:bg-green-600 text-white font-bold py-3 px-6 rounded-full text-base transition shadow">
          <i class="fab fa-whatsapp mr-2"></i> Enviar interpretación por WhatsApp
        </a>
        <p class="mt-3 text-sm text-gray-600">Al hacer clic, se abrirá WhatsApp. Solo debes presionar "Enviar".</p>
      `;

      resultSection.classList.remove('hidden');
    });
  </script>
</body>
</html>

